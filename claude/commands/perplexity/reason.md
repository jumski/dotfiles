---
argument-hint: <complex question requiring analysis>
description: Deep reasoning and analysis using Perplexity Reason
---

Use `mcp__perplexity__perplexity_reason` to analyze: "$ARGUMENTS"

If no arguments provided, provide deep reasoning about the current discussion topic.

**Parameters:**
- `strip_thinking: true`

**Query tips:** Include detailed context - constraints, trade-offs, specific requirements. For debugging, include error messages, stack traces, code snippets. More context yields better reasoning.
