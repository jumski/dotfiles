prepare-commit-msg:
  scripts:
    "generate-commit-msg.sh":
      runner: bash
      output:
        - meta
        - summary
        - error

# commit-msg:
#   commands:
#     "lint commit message":
#       run: pnpm commitlint --edit {1}

post-commit:
  commands:
    "push changes to origin":
      run: git push
