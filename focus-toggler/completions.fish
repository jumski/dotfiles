# Completions for focus-mode command

complete -c focus-mode -f

# Mode options with descriptions
complete -c focus-mode -a "activities" -d "Toggle activities and windows"
complete -c focus-mode -a "windows" -d "Toggle windows only"
complete -c focus-mode -a "kitties" -d "Toggle between kitty-remote and kitty-local"
